# Copie este ficheiro para "server/.env" e personalize os valores conforme o seu ambiente.
# O backend usa SQLite local (arquivo server/data/database.db), então não há nada para configurar aqui.

NODE_ENV=development
PORT=4000

# Credenciais do painel administrativo
ADMIN_EMAIL=admin@amigoocuto.com
ADMIN_PASSWORD=troque-esta-senha-segura
ADMIN_JWT_SECRET=troque-este-segredo-super-seguro
ADMIN_SESSION_MINUTES=120

# Configurações de e-mail
# Use MAILER_MODE=console para desenvolvimento (imprime no terminal)
# Troque para MAILER_MODE=smtp e preencha os campos abaixo para enviar e-mails reais.
MAILER_MODE=console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=o-teu-email@gmail.com
SMTP_PASS=a-tua-senha-de-app-de-16-letras
MAIL_FROM="Amigo Ocuto" <o-teu-email@gmail.com>
